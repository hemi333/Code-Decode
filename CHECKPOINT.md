# 1주차 체크포인트

`python check.py` 가 통과했다면 코드는 됩니다. 이 문서는 **이해**를 확인합니다.
둘은 다릅니다. 아래 질문에 막힘없이 답할 수 있으면 이번 주가 끝난 것입니다.

모임에서 한 사람이 답하고 나머지가 반박하는 식으로 진행하면 좋습니다.

---

## A. 응답 구조

**A1.** `response.content` 는 왜 문자열이 아니라 리스트입니까?
리스트여야만 하는 상황을 하나 말해보세요.

**A2.** `stop_reason` 이 가질 수 있는 값을 아는 대로 적고, 각각 무슨 뜻인지
설명하세요. `end_turn` 과 `max_tokens` 를 구분하지 못하면 어떤 버그가 생깁니까?

**A3.** 2단계에서 도구를 붙였을 때와 안 붙였을 때 모델의 답이 어떻게 달랐습니까?
도구 없이 물었을 때 모델이 그럴듯한 버전 번호를 지어냈다면, 그건 모델의
결함입니까 아니면 우리가 잘못 물어본 것입니까?

---

## B. 루프

**B4.** 모델은 지난 대화를 기억합니까? 그렇다면/아니라면, 여러 턴 대화가
가능한 이유는 무엇입니까?

**B5.** `messages` 에 assistant 응답을 붙이지 않으면 무슨 일이 일어납니까?
직접 지워보고 확인한 다음 답하세요.

**B6.** `tool_result` 의 `role` 이 `"tool"` 이 아니라 `"user"` 인 이유를
설명해보세요. 이 설계가 보안 관점에서 무엇을 시사합니까?

> 힌트: 도구 결과가 user 턴이라는 건, 도구가 돌려준 내용이 **신뢰할 수 있는
> 지시가 아니라 검증이 필요한 입력**이라는 뜻입니다. 5주차에 외부 문서를
> 긁어오기 시작하면 이 구분이 실제 문제가 됩니다.

**B7.** `tool_use_id` 는 왜 필요합니까? 도구를 한 번에 하나만 부른다면
없어도 되지 않습니까?

**B8.** `MAX_TURNS` 없이 루프를 돌리면 어떤 상황에서 멈추지 않습니까?
그런 상황을 실제로 만들어낼 수 있습니까?

---

## C. 비용과 설계

**C9.** 도구를 한 번 부르는 대화는 API 를 **몇 번** 호출합니까?
`messages` 는 매번 통째로 다시 보내지므로, 턴이 늘면 입력 토큰은
어떻게 증가합니까? (선형입니까, 그보다 빠릅니까?)

**C10.** 도구의 `description` 을 한 줄로 줄였을 때 모델이 도구를 안 부르는
경우가 있었습니까? 그렇다면 `description` 은 문서입니까 프롬프트입니까?

**C11.** `count_lines` 에서 마지막 개행을 어떻게 처리하기로 정했습니까?
그 선택의 근거를 코드 주석에 남겼습니까?

---

## D. 다음 주로 넘기는 질문

**D12.** 이번 주 도구는 입력이 코드 문자열 하나였습니다.
4주차에 만들 `analyze_code` 는 코드를 받아 "학습 대상"을 뽑습니다.
그 반환 형태는 어떻게 생겨야 할까요? 지금 한 번 설계해보세요.
4주차에 자기가 적은 것과 비교하면 재미있습니다.

**D13.** 다음 코드를 모델에게 주면, 모델은 `.glob` 이 무엇인지 알 수 있습니까?

```python
sorted(GEN_IMAGES_DIR.glob("*.png"))
```

알 수 있다면 어떻게, 없다면 왜 없습니까? 이 질문이 5주차의 전부입니다.

---

