# 2주차 체크포인트

`python check.py` 가 통과했다면 코드는 됩니다. 이 문서는 이해를 확인합니다.

1주차와 같은 방식으로 하세요. 한 사람이 답하고 나머지가 반박합니다.

---

## A. 병렬

**A1.** 모델이 한 응답에서 도구를 세 개 부를 때, `tool_result` 세 개를
**하나의** user 메시지에 담아야 합니다. 세 개의 메시지로 나누면 무슨 일이
생깁니까? 직접 나눠보고 확인한 다음 답하세요.

**A2.** 도구 세 개 중 하나만 실행하고 나머지를 무시하면 어떻게 됩니까?
API 는 왜 그걸 허용하지 않습니까?

**A3.** 병렬 호출이 가능하면 순차 호출보다 무엇이 이득입니까?
왕복 횟수가 줄면 **비용**은 어떻게 달라집니까?
(힌트: `messages` 는 매번 통째로 다시 보냅니다)

---

## B. 실패

**B4.** `{"error": "..."}` 라는 JSON 을 그냥 넘겨도 모델은 대충 알아챕니다.
그런데도 `is_error` 를 쓰는 이유가 뭡니까?

**B5.** 도구가 예외를 던지면 루프가 멈춥니다. 그래서 `dispatch` 가 예외를
값으로 바꿉니다. 그런데 **모든** 예외를 삼키는 게 항상 옳습니까?
삼키면 안 되는 예외의 예를 하나 들어보세요.

**B6.** 없는 파일을 읽으라고 했을 때, 모델이 그 실패를 어떻게 처리했습니까?
로그를 보고 확인하세요. 실패를 무시하고 답했습니까, 언급했습니까?
언급하게 만들려면 어디를 고쳐야 합니까?

---

## C. 예산과 종료

**C7.** `MAX_TURNS` 와 `MAX_REPEATS` 는 각각 무엇을 막습니까?
둘 중 하나만 있으면 어떤 상황을 놓칩니까?

**C8.** 호출 예산에 걸려 실행을 건너뛸 때도 `tool_result` 는 만들어야 합니다.
왜 그렇습니까?

**C9.** 서명을 `f"{name}:{json.dumps(input, sort_keys=True)}"` 로 만들었습니다.
`sort_keys=True` 가 없으면 무엇이 잘못됩니까?

---

## D. 신뢰 경계 ← 이번 주의 핵심

**D10.** `read_file(name)` 의 `name` 은 누가 만든 값입니까?
여러분이 넘긴 값입니까?

**D11.** 이름에 `/` 나 `..` 가 있는지 검사하는 것만으로는 부족합니다.
왜 부족합니까? 어떤 경우에 뚫립니까?

> 직접 확인해보세요.
> `ln -s ../agent.py fixtures/sneaky.py` 를 만들고 읽어보면 됩니다.

**D12.** "무엇을 막을지 나열하기" 와 "무엇만 허용할지 확인하기" 중
어느 쪽이 안전합니까? 왜 그렇습니까?

**D13.** 1주차 B6 에서 도구 결과의 role 이 `"user"` 인 이유를 물었습니다.
이번 주에 그 답이 실감났습니까? 도구 결과가 신뢰할 수 없는 입력이라면,
6~7주차에 **인터넷에서 문서를 긁어오기 시작하면** 무엇을 조심해야 합니까?

---

## E. 다음 주로

**E14.** 이번 주 도구는 셋이었고 서로 독립적이었습니다.
4주차의 `analyze_code` 와 5주차의 `lookup_documentation` 은 **연쇄**됩니다.
분석 결과가 문서 조회의 인자가 되니까요.

그때 `analyze_code` 가 심볼 10개를 뱉으면 `lookup_documentation` 을
10번 부를까요, 한 번에 10개를 넘길까요? 어느 쪽이 낫습니까?
이번 주에 배운 병렬/비용 이야기로 답해보세요.

---

## 제출

`week02/answers/<이름>.md` 로 올리세요.
답이 갈리는 문항이 다음 모임의 첫 안건입니다.
